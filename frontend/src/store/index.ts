import { createStore } from 'vuex'
import ClientStorage from '@/store/ClientStorage';

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    ClientStorage
  }
})
